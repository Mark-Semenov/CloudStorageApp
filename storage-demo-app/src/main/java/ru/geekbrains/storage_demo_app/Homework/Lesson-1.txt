ДЗ № 1: Ответ на вопросы

1. Как организовать клиент-серверное взаимодействие?

Я хотел бы попробовать технологии JavaEE т.к. у нас заканчивается курс и интересно применить знания на прктике.
Стек:
JavaEE(JSF, CDI, Servlet API, JPA (Hibernate)), EJB)
WildFly
PostgreSQl

2. Как и в каком виде передавать файлы?

Не совсем понимаю с точки зрения архетиктуры где хранить файлы?
Как это просходит в рельности?
Ну т.е. если есть сервер у него есть HDD то храним в файловой системе так?
В нашем случае мы просто будем перекладывать условно из папки в папку?
Полуается как бы муляж сетевого хранилища, если я правильно мыслю.

В общем пока на ум приходит передача файлов в виде массива байтов.
Записывать буду в базу данных (хотя это вроде как не совсем верно).

Про JSON, BSON ничего к сожалению не пока не знаю.
Если будет время обязательно ознакомлюсь.


3. Как пересылать большие файлы?

    Возможно, делить их на пакеты и потом собирать.


4. Как пересылать служебные команды?

    через http

5. Что хранить в базе данных?

Информцаию о пользователях (логин, пароль и тд).
Если хранить файлы где-то, то писать в базу пути к файлам.

6. Как передавать структуру каталогов/файлов?

В виде текстовой информации имя_каталога/имя_файла

7. Какую библиотеку использовать для сетевого взаимодействия: java.io, java.nio, Netty?
   Очень интересно изучить Netty.